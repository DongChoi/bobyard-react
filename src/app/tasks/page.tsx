import React from "react";

const tasks = () => {
  // const []

  return (
    <div>
      welcome
      {`due today {} && if no todos, "you have no more tasks today! confetti {add option to add tasks}"
      you are one fire! we see that you have overdue tasks, would you like to knock out one more? or take on upcoming challenges to get a head start for tomorrow?
      due upcoming after today
      overdue tasks`}
    </div>
  );
};

export default tasks;
